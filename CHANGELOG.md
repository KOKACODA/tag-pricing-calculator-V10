# KOKALabel 报价系统 变更日志

## v9.8.0（2026-09-09）

### 数据管理新增「在线修改」标签页：报价表组/报价表在线管理 + 管理员按模板改数据

- **新增「在线修改」标签页**（个人主页 · 数据管理，位于「报价历史」与「云同步」之间）：区块一「报价表组与报价表管理」——按组渲染卡片（组名 / 报价表数 / 重命名组 / 新增报价表 / 删除组），组内报价表表格支持重命名、删除（联动清理纸张与工艺）、归属组下拉直接移动；区块二「按模板修改报价数据（管理员）」——选定目标报价表 → 导出含现有数据的模板 → 修改后导入即原地覆盖该报价表（`applyPriceListData` 先清旧纸张/工艺再合入新数据，不再像价格配置页那样「导入 = 新增报价表」）；模板内改「总报价表」名会同步重命名目标报价表。
- **权限门控**：http 部署时仅管理员可用（非管理员显示锁定提示、隐藏管理区），`file://` 本地离线（无登录体系）视为机主放行；访客本就无个人主页入口。
- **数据层新增**（`js/data.js`）：`PRICE_LIST_GROUPS` 持久化存储 + `getPriceListGroups` / `getGroupName` / `addPriceListGroup` / `renamePriceListGroup`（同步迁移 `GROUP_NAME_MAP` 保证 Excel 导出元信息一致）/ `deletePriceListGroup`（仅空组且非最后一组）/ `renamePriceList` / `movePriceListToGroup` / `applyPriceListData`；`addPriceList` 增加可选 `groupId` / `switchTo` 参数（在线管理区新增不抢当前报价表焦点），并修复 ID 纯时间戳同毫秒碰撞（追加随机后缀，`addPriceListGroup` 同）。
- **组配置全链路覆盖**：本地备份 / 完整配置 JSON 的导出与导入、快照保存与恢复、重置为默认配置 / 恢复全局默认 / 清除缓存、管理员「上传默认数据」云下发（`auth.js` 应用 `priceListGroups`）均包含组配置；导入数据校验 `validateImportedData` 新增组结构校验；旧备份/旧快照无该字段时保留现状（向后兼容）。
- **修复**：`paperToSheetRows` 参数化（可导出任意报价表模板，不再硬编码当前报价表）；导出元信息「所属小组」改用动态 `getGroupName()`。
- 测试：新增 `tests/online-edit.test.mjs`（12 用例：组/表 CRUD 与持久化、改名重名校验、删除守卫、`applyPriceListData` 覆盖语义、UI 结构与事件绑定、组配置全链路覆盖），全站 57/57 通过。
- 版本号同步：全站升至 v9.8.0。

## v9.7.3（2026-09-09）

### 1 楼默认报价表整体变更（28 组 → 33 组）+ 迁移守卫版本比较修复

- **1 楼默认报价表替换**：按最新 Excel（KOKALabel1楼_20260908_2227.xlsx）整体替换 1 楼默认数据为 33 组（id paper2_1~paper2_34，缺 paper2_30）。与 v9.0.1 的 28 组相比：多材质合并纸拆分（600白纹/600黑卡/600牛皮、手柔皮系等独立成组，拆分后简称合法，不再依赖 v9.7.2 的 Sheet 名清洗兜底）；新增 5 组（85C白卡纸 / 200双铜纸 / 200厚胶带 / 巴黎白彩透卡 / 900白金卡为新序号位）；1 楼直接系数占位改为显式 `null`（原 max/min 全 null 的空壳占位在导出/导入时不可用）。3 楼 10 组默认数据原样保留。
- **迁移守卫修复（关键）**：v8.4 / v9.0.1 / v9.7.3 三处迁移守卫原为 `===` 等值判断——dataVersion 已是 "9.0.1" 的老用户每次加载页面时，v8.4 迁移都会重跑并把全部纸张/工艺重置为默认，**抹掉 3 楼用户改价**，随后 v9.0.1、v9.7.3 也跟着重跑（v9.0.1 上线以来即存在此隐患）。新增 `compareDataVersion()` 语义化版本比较（空串视为最旧），三处守卫改为 `>= 0` 跳过，高版本用户不再重跑低版本迁移；v9.7.3 迁移确保：1 楼替换为新 33 组、3 楼（含用户改价）保留、1 楼工艺按新默认重建。
- 测试：新增 `tests/floor1-data.test.mjs`（默认 33 组结构与抽查价格 / 工艺段 / 老用户迁移保 3 楼改价 / 幂等），全站 45/45 通过。
- 版本号同步：全站升至 v9.7.3。

## v9.7.2（2026-09-08）

### 1 楼报价表导出失败修复 + 附加工艺折叠标题红色高亮

- **1 楼报价表导出失败修复**：根因是 1 楼 6 张多材质合并纸张的简称含半角 `/`（如「600白纹/600黑卡/600牛皮/800纹棉」），其中 2 张还超过 Excel Sheet 名 31 字符上限（36/35 字符）——SheetJS 追加 Sheet 时抛错「Sheet name cannot contain : \ / ? * [ ]」，整表导出中断。与 900 克铜版纸数据修正无关（该纸数据结构正常）。新增 `toSafeSheetName()` 清洗函数（半角 `: \ / ? * [ ]` → 全角等价、超 31 字符截断、清洗后重名追加序号），「导出当前报价表」与「下载空白模板」两处导出均已接入；导入侧按 Sheet 内容元信息（报价表全称/简称）匹配而非 Sheet 名，回导不受影响。3 楼报价表因简称全部合法此前即可正常导出。
- **附加工艺折叠标题红色高亮**：v9.5.2 只对「吊绳类型」做了标题红显，本次将规则统一为 `#page-calculator .collapse-toggle`——「附加工艺」折叠标题同样 15px 加粗红色（#b91c1c，hover #991b1b），吊绳行为不变（值相同）。
- 测试：新增 `tests/export-sheet-name.test.mjs`（非法字符映射 / 截断 / 重名序号 / 空名回退 / 合法名保留），全站 40/40 通过。
- 版本号同步：全站升至 v9.7.2。

## v9.7.1（2026-09-08）

### 文档全面更新 + 仓库目录整理 + 发版流程固化

- **五文档更新**（AGENTS.md / docs/项目历史与技术总档案.md / docs/HANDOFF-v8.md / docs/项目总结.md / CHANGELOG.md）：全部对齐 v9.7.1 现状——AGENTS 增补仓库改名与直传发版说明、文件地图（assets/、login.html、函数索引新增公共计算函数）；HANDOFF 更新文件结构（补 login.html / auth.js / login.js / wrangler.toml / assets / docs 全列表）、上线流程改为 wrangler 直传、修正过时的「默认分支仍是 main」描述；项目总结新增「十、增量更新（v8.9.0 → v9.7.1）」章节衔接快照；总档案新增「增量记录（v9.5.0–v9.7.1）」大事记速览。
- **仓库一级目录整理**：站点图标 `favicon.ico` / `favicon.png` / `apple-touch-icon.png` 从根目录下沉至新建的 `assets/`；`index.html` 引用同步改为 `assets/…?v=9.7.1`，`login.html` 补齐此前缺失的 favicon 显式引用。根目录其余文件（`_headers` / `robots.txt` / `wrangler.toml` / `README` / `CHANGELOG` / `AGENTS.md`）按平台与生态惯例必须留根，不动。
- **误删检查**：`git log --diff-filter=D` 确认本谱系历史上无任何文件删除记录，无过往误删。
- 版本号同步：全站升至 v9.7.1。

## v9.7（2026-09-08）

### 临时修改计算统一单源 + 历史列表事件委托 + 删除记录后统计同步刷新

- **计算统一单源（防「显示价 ≠ 保存价」暗雷）**：新增两个公共计算函数，屏幕渲染与保存记录共用同一份公式，将来改计算规则只需改一处：
  - `computeStandardOverridePrice(result, values)`：标准模式临时毛利系数/邮费快速修改的价格计算（双算法价、仅邮费各级重算等分支），`renderCustomCoeffCard` / `renderShippingOverrideCards`（屏幕卡片）与 `collectStandardOverride`（保存记录）统一调用。
  - `computeDirectTempTotals(result, rawValues)`：直接模式每纸临时系数的计算（含批量直接价、无效系数、修改后总价、modified 标记），`renderTempCoeffResults`（屏幕明细）与 `collectDirectTempOverride`（保存记录）统一调用；算式文本拆到纯展示函数 `buildTempCoeffCalcStr`。
  - 重构为纯函数行为等价改造：`node --check` + 全量测试通过，保存的 `snapshot.override` 数据格式与 v9.6 完全一致，旧记录读取不受影响。
- **历史列表事件委托**：`renderHistory` 由「逐行 addEventListener（每行 3 个按钮）」改为**表格级单一 onclick 委托**（赋值天然幂等，重建 innerHTML 不叠加监听），历史记录几百条时渲染明显更快；disabled 按钮本身不触发 click，无需额外过滤。
- **修复：删除历史记录后统计报表不刷新**：原逐行删除逻辑未调用 `renderStats()`，删除后统计（月度次数/热销尺寸/利润分布）停留在旧数据直到下次进页；v9.7 删除后立即刷新。
- **勘误**：v9.6 提出的优化建议第 2 条「载入历史不回填临时修改」经查证为**误报**——载入回填自报价历史初版（`f738d9e`）即已实现（`inputs.customCoefficient` / `inputs.shippingOverride` / `inputs.tempCoefficients` 回填输入框后重算），本次仅验证未改动。
- **测试**：`tests/quote-history.test.mjs` 新增 2 用例——`computeStandardOverridePrice` 四分支（无修改/仅系数/双修改/仅邮费）、`computeDirectTempTotals`（temp 计算/默认值未修改/无效系数/批量直接缺价），全套 36 用例通过。
- 版本号同步：全站升至 v9.7（index.html / data.js / app.js / style.css / login.html / README / AGENTS / HANDOFF-v8）。

## v9.6（2026-09-08）

### 保存报价时固化临时修改（临时毛利系数 / 邮费快速修改 / 每纸临时直接系数）

- **问题**：调整了「临时毛利系数」或「邮费快速修改」（或两者都改）后点保存报价，这些临时修改不随记录保存——历史列表价格仍显示原价，详情里也看不到当时的修改内容。
- **修复**：
  - 保存报价时收集当前生效的临时修改并计算修改后价格，写入历史记录快照 `snapshot.override`（深拷贝，保存后再次修改输入不影响已存记录）：
    - 标准模式：临时毛利系数（含双算法价 price1/price2）、邮费快速修改（新邮费 + 各客户等级修改后价）、两者同时生效（算法1 邮费一起乘系数 / 算法2 邮费不乘系数）。
    - 直接系数模式：每纸临时直接系数（含默认值对比标记、批量直接价、无效系数标记、修改后总价与「部分缺价」状态）。
  - 历史列表「报价」列、统计报表利润分布优先读取**修改后价格**（override 存在且价格有效时）；旧记录（无 override）行为完全不变，向后兼容。
  - 历史详情弹窗新增「临时修改（保存时生效）」红色边框区块：展示生效的临时系数 / 新邮费 / 修改后成本与各级报价 / 双算法价 / 每纸临时系数明细与修改后总价。
  - 兼容性：无任何临时修改时 `snapshot.override` 不写入（不增加旧记录体积）；override 数据损坏或价格非法时自动回退原价，不抛错。
- **测试**：`tests/quote-history.test.mjs` 新增 2 个用例——override 写入与深拷贝、历史价格读取优先返回修改后价（含 6 种分支：无 override / 系数+邮费 / 仅系数 / 仅邮费 / 直接模式 / 非法值回退）。
- **修复 bug（本次测试中发现）**：`getHistoryRecordPrimaryPrice` 中 override 无有效价格字段时 `Number(null)===0` 被误判为有效价返回 0，现先判空再转数字。
- 版本号同步：全站升至 v9.6（index.html / data.js / app.js / style.css / login.html / README / AGENTS / HANDOFF-v8）。

## v9.5.3（2026-09-08）

### 结果表单价列恢复面积系数算式显示

- **面积系数算式显示**：结果表「各纸张尺寸 / 面积 / 代码」的**单价列**在面积超过 10000 mm²、报价表无对应档位而启用面积系数时，恢复显示完整算式「¥ 基础价 × 系数 = ¥ 实际价」（例：面积 15759 mm² → 系数 1.58 → `¥ 85.00 × 1.58 = ¥ 134.30`，结果蓝色高亮），与下方「纸张价合计」行的既有格式一致；面积系数为 1（未超 10000）时仍显示纯单价。
- 版本号同步：全站升至 v9.5.3。

## v9.5.2（2026-09-08）

### 切换按钮顺序调整 + 吊绳红色高亮对象修正为折叠标题

- **切换按钮顺序**：参数输入右上角「标准报价」按钮移到「直接系数计算」之前（标准报价在前），默认仍是标准报价。
- **吊绳红色高亮对象修正**：v9.5.1 误将红色加粗样式加在吊绳**选项**（`.rope-item`）上，本次移除；改为给折叠标题「吊绳类型 点击展开」（`.rope-toggle`，含提示文字）加大字体（15px）、加粗、红色（`#b91c1c`），hover 为深红（`#991b1b`）。吊绳选项恢复默认 13px 样式；选项右上角的初始价格标记（`.option-initial-price`，红色小字）保持原有高亮不变。
- 版本号同步：全站升至 v9.5.2；README / HANDOFF-v8 分支表同步（此前停留在 v9.5.0）。

## v9.5.1（2026-09-08）

### 尺寸类型移入个人主页 + 默认模式改为标准报价 + 吊绳红色高亮

- **尺寸类型移入个人主页**：「单张尺寸/展开尺寸」下拉选择从智能计算器每张纸的尺寸输入行移除，改为「个人主页-报价设置-默认选项」下的全局默认值。新建纸张时自动使用该默认值，已保存的快照/历史中的 `sizeType` 字段不受影响，结果表仍正确显示「展开」标记。
- **默认显示模式改为标准报价**：`calcMode` 默认值从 `"direct"` 改为 `"standard"`，页面切换按钮相应调换 active 状态。
- **吊绳类型字体加大红色高亮**：参数输入区的吊绳选项字体增大至 15px、加粗、红色（`#b91c1c`），仅计算器页面生效，不影响报价表编辑区的吊绳价格表。
- 清理：移除所有残留的 `size-type-group` / `sheet-size-type` CSS 选择器和 HTML 模板，grid 布局从三栏改为两栏。
- 文档：HANDOFF-v8、README 分支版本同步更新为 v9.5.1。

## v9.5.0（2026-09-08）

### 账号登录体系（管理员 / 业务员 / 访客）+ 默认数据上传 + 折叠区

- **默认数据修正**：`1楼小组-22` 由「900克 A级蒙版纸」改为「900克 A级铜版纸 双面过哑胶（厚度1.05mm）」，`shortName` 同步为 `900铜版纸`。
- **账号体系上线**（Cloudflare Pages Functions + KV，本地 `file://` 离线打开不受影响）：
  - 角色三级：`admin`（管理员，超级权限）/ `sales`（业务员）/ `guest`（访客）。
  - 首次初始化：登录页在系统未初始化时显示「初始化密钥」，携带正确 `SETUP_KEY` 即创建默认管理员（账号 `KOKA`，由部署变量 `ADMIN_USER/ADMIN_PASS` 指定）并**直接返回会话**，无需二次登录。
  - 管理员可新增任意角色子账号、修改自身与子账号的**昵称/密码**、设置使用时长（天）、启停、删除（不能删除/停用自己）。
  - **默认保留一个管理员**：停用或删除管理员时强制至少保留 1 个启用管理员（`countActiveAdmins` 保护）。
  - **启停 bug 修复**：前端按钮文本=将要执行的动作（显示「停用」→ `enabled:false`；显示「启用」→ `enabled:true`），后端登录/会话双重校验 `enabled`，停用后立即无法登录。
- **默认数据上传（仅管理员）**：管理员在「报价表组查询」可将当前全部配置（报价表组/纸张/工艺/吊绳/邮费/客户等级）一键上传为全用户共享默认数据；其他账号下次登录自动应用并重载页面；业务员/访客无上传入口且后端 `POST /api/default` 拒绝非管理员。
- **页面访问控制**：访客仅保留「智能报价计算器」，隐藏「报价表组查询」「个人主页」导航与页面；业务员可见全部页面但不可上传/修改默认数据。
- **UI 折叠**：智能计算器「附加工艺」「吊绳类型」默认折叠，点击标题展开（兼容动态生成的纸张卡片，事件委托）。
- 安全：密码 PBKDF2-SHA256(10万次) + 随机盐；会话 7 天 KV 过期；API 全程 Bearer 鉴权；`session.js` 对停用/过期账号即时失效会话。

## v9.4.2（2026-09-05）

### main 分支同步 + 项目历史档案 + 文档目录规范

- 将 `main` 分支 force 同步到 `v8`（v9.4.2），消除长期并存的旧 v7.10 谱系分叉；旧谱系历史经本地备份 tag `archive/v7.10-main` 保留可追溯。
- 新增 `docs/项目历史与技术总档案.md`：一份覆盖开发史、技术架构、转手方案、已知问题、方法论反思的全量档案，作为项目文档总纲。
- 规范文档目录：在 `README.md` 与 `AGENTS.md` 中补齐新档案索引，修正分支说明表中已过时的 `main = v7.10 存档` 描述。
- 全站版本号统一为 v9.4.2（`index.html` 标题与资源引用、`js/data.js`、`js/app.js`、`css/style.css` 版本头、`README`）。

## v9.4.1（2026-09-05）

### 快照与统计报表合并框体 + 导出 + 清除缓存

- 将「本地快照列表」从数据管理「本地快照」标签页抽出，与「统计报表」合并为独立的「快照与统计报表」卡片，置于个人主页「数据管理」下方；原「本地快照」标签更名「价格配置」。
- 统计报表由弹窗改为**内联渲染**（进入个人主页即自动刷新），移除原「更多功能 → 统计报表」占位卡与 `statsDialog` 弹窗。
- **新增导出**：`导出快照`（带完整性签名的 JSON）、`导出统计报表`（XLSX 三工作表：月度报价次数 / 热销尺寸 / 利润分布），均仅下载到本机、不联网。
- **新增「一键恢复默认并清除缓存」**：在恢复全局默认基础上，额外删除报价历史（`history`）与本地快照（`snapshots`）。
- **保留「一键恢复全局默认设置」**：仍不删除报价历史与本地快照；两按钮在「全局恢复」区并列，文案已区分。
- 利润分布分桶逻辑抽取为 `PROFIT_BRACKETS` + `computeProfitBrackets`，供渲染与导出复用。

## v9.4.0（2026-09-05）

### 统计报表（本地，不上传）

- 「更多功能」中的「统计报表」占位卡片启用：点击打开独立的统计框体（`statsDialog`）。
- 基于本地已保存的报价历史（`localStorage.history`）实时汇总，三项指标：
  - **月度报价次数**：按 `createdAt` 归集到 `YYYY-MM`，横向条形图。
  - **热销尺寸**：按吊牌展开尺寸「宽×长（mm）」聚合，取前 8 名。
  - **利润分布**：`建议报价 − 成本`，按「亏损 / 0~50 / 50~100 / 100~200 / 200+」分桶，并展示有效记录数、平均/累计/最高/最低单笔利润。
- 纯前端渲染（CSS 条形图），零第三方依赖，完全符合 CSP `script-src 'self'` 与离线双击使用；数据只读写本机 `localStorage`，绝不联网上传。
- 旧记录兼容：尺寸优先取 `inputs.sheets`，缺失时回退 `snapshot.sheetDetails`；利润仅统计同时含有效成本与报价的记录，缺价记录自动跳过。

### 测试

- 新增 `tests/stats.test.mjs`（尺寸解析 / 尺寸提取回退 / 月度·尺寸·利润聚合 / 缺价跳过 / 条形图渲染 / 利润分桶），全量单测 24 项全绿。

## v9.1.0（2026-09-05）

### 合并安全加固（P0+P1）与 v9.0.1 数据基线

- 将此前在 v8 分支完成的安全加固并入 v9.0.1 数据基线：XSS 转义修复、导入校验加固、HTTP 响应头强化（P0）；数据防篡改签名 + 前端口令门（P1）。
- 1楼报价表为最新 **28 个报价组**（v9.0.1）；3楼 10 张保持不变。
- 全站版本号统一为 v9.1.0。

## v9.0.1（2026-09-04）

### 1楼报价表再次更新（29 组 → 28 组）

- 按最新附件 `KOKALabel1楼_20260904_1531(1).xlsx` 更新 1楼报价表：29 个报价组 → **28 个报价组**（全称编号 1楼小组-1~28 连续）。
- 各组含独立直接系数、批量报价、规格矩阵与工艺；**3楼 10 张保持不变**。
- 迁移条件更新为 `dataVersion=9.0.1`（`migrateV9ReplaceFloor1`），旧数据升级时保留 3楼（含用户自定义改价），仅替换 1楼。
- 纸张 id 重排 `paper2_1 ~ paper2_28`。

## v9.0.0（2026-09-04）

### 1楼报价表替换为最新 Excel（34 张 → 29 个报价组）

- 按附件 `KOKALabel1楼_20260904_1531.xlsx` 重建 1楼报价表：34 张纸 → **29 个报价组**（纸张按材质组合重新分组，如「白纹卡600/黑卡纸600/牛皮纸600/纹棉纸800」一个报价组涵盖多种材质）。
- 各组含独立直接系数、部分批量直接报价、规格矩阵与工艺。
- **3楼 10 张保持不变**（含用户在系统里的自定义改价，迁移时保留）。
- 1楼工艺按附件重建；新增 `migrateV9ReplaceFloor1` 迁移（`dataVersion=9.0`），旧 localStorage 数据平滑升级。
- 纸张 id 重排为 `paper2_1 ~ paper2_29`。

### 新增临时系数第二种算法显示

- 同时填写「临时毛利系数 + 邮费快速修改」时，除了原「(成本 − 原邮费 + 新邮费) × 系数」（邮费乘系数），新增并排显示 **「(成本 − 原邮费) × 系数 + 新邮费」（邮费不乘系数）**，用绿色实线卡片区分，方便两种报价口径对比。

### 版本号统一

- 所有文件版本号统一为 v9.0.0（含修正此前残留的 `v7.9`/`v7.8`/`8.1.0` 旧版本号）。

## v8.11.0（2026-09-04）

### 数据防篡改（P1）

- **导出完整性签名**：完整配置 / 本地备份导出时经 `signExport` 对数据体计算 `sha256Hex(canonicalJson(payload))` 写入 `meta.integrity`；导入时重算比对，篡改或损坏即阻断并提示；旧版无 `integrity` 字段的备份仍可恢复，并提示「无完整性校验」。
- **稳定序列化 + SHA-256**：`data.js` 新增纯 JS 同步实现 `canonicalJson`（对象键升序、数组保序、去空白）与 `sha256Hex`（无依赖，HTTPS 与离线 file:// 均可用）。

### 访问控制 · 前端口令门（P1）

- `index.html` 增加全屏口令遮罩，`app.js` 启动时校验 `sessionStorage["tagPricing_unlocked"]`，未解锁先要求口令（`sha256Hex(ACCESS_SALT + 口令)` 比对）再初始化界面；解锁仅写 sessionStorage（关页失效），不做持久免密。
- `data.js` 顶部新增 `ACCESS_ENABLED / ACCESS_SALT / ACCESS_PASSCODE_HASH` 占位（默认 `ACCESS_ENABLED=false` 关闭，附生成哈希与替换说明）。此门只防「打开直接看到」，真正保密需在 Cloudflare Access 开边缘鉴权。

### 测试

- 新增 `tests/integrity.test.mjs`（SHA-256 标准向量 + canonicalJson + 导出签名/导入校验回归），全量单测 18 项全绿。

## v8.10.0（2026-09-04）

### 安全加固（P0）

- **XSS 转义修复**：升级 `escapeHtml` 补齐双引号/单引号转义（同时适配属性值场景）；Excel 导入 / 吊绳导入 / 邮费导入 / 本地备份恢复等状态提示涉及的报价表名、纸张简称、工艺名、吊绳名、地区名、错误信息、文件名等动态内容全部经 `escapeHtml` 转义后再拼入 HTML，堵死存储型 XSS。
- **导入校验加固**：`parsePaperExcel` 的规格/工艺/直接系数/批量直接报价价格全部拒绝负数与 `NaN`/`Infinity`（新增 `isNonNegFinite`）；规格档位去重并升序排序，防止表头乱序/重复导致价格错位。
- **文件大小上限**：纸张/吊绳/邮费 Excel、完整配置、本地备份五个导入入口统一限制 50MB，防止超大文件导致卡死或内存耗尽。
- **Excel 公式注入防护**：导出 Excel 的名称/代码单元格（报价表名、纸张简称、规格代码、工艺名、吊绳名、地区名）经 `safeExcelText` 处理，`=`/`+`/`-`/`@` 开头自动加单引号转文本。
- **导入数据深校验**：`validateImportedData` 扩展为对报价表组/客户等级/纸张/吊绳/工艺/邮费/规格价格等全结构深校验（类型、非负、长度、数量上限），拦截恶意或损坏的备份 JSON。
- **响应头强化**：`_headers` 新增 `Strict-Transport-Security`（HSTS）与完整版 `Content-Security-Policy`（补 `base-uri`/`frame-ancestors`/`object-src`/`worker-src`/`font-src`/`form-action`）。

## 维护（2026-09-04 · 无版本号变更）

- 补全 `docs/main-branch-summary.md` 至**论文级完整历史档案**（约 280 行 → 1161 行）：从 git 历史恢复三份被删归档文档 `文档1-版本修改总记录.md`（297 行）/ `文档2-完整流程文档.md`（485 行）/ `文档3-大总结与技术分析.md`（233 行，共 1015 行），整合出 pre-v4.3 前史（v1.0 前身 2026-07-28 → v4.2.1 2026-08-05）逐版本明细 + 技术架构深度分析（五区代码结构 / 三级数据模型 / calculate 九步流程 / Excel 导入导出 / 存储状态 / 部署流程）+ 设计决策与技术债 + 版本演进方法论 + main↔v8 关系与历史查阅索引
- 修订该档案中 main 历史描述为全谱系「2026-07-28 起点 → v7.10 终点」，不再局限于 v4.3→v7.10

## 维护（2026-09-03 · 无版本号变更）

- 新增 `docs/HANDOFF-v8.md`（项目转手/交接文档），覆盖 v8.9.0 全貌、main↔v8 双分支关系、部署与维护注意事项，取代旧 HANDOFF-v5/v6
- 关联：`main` 分支同步完成旧文档清理（删除含 account_id 的 HANDOFF-v5 等冗余文档）并记录日志
- 新增根目录 `AGENTS.md`（新 agent 接手「只读入口」，约 60 行）：给出文件地图 + 行数 + 「按任务只读局部」决策、函数索引、验证命令，降低首次接手的 token 消耗
- 新增 `README.md`（v8 分支此前无 README，本次补齐）：项目定位 / 线上地址 / 功能 / 分支说明 / 结构 / 验证 / 文档索引
- 新增 `docs/main-branch-summary.md`（main 分支 v7.10 旧谱系超级详细历史档案）：版本时间线总览 + 逐版本详细变更（2026-07-28 起点 → v7.10 终点，含 pre-v4.3 前史）+ 架构/数据模型/安全/性能四大演进主线 + main↔v8 双分支关系 + 历史查阅操作索引，供课题研究与跨 agent 快速读取旧数据

## v8.9.0（2026-09-03）

### 新增「吊牌成本合计」

- 在结果区「邮费」行上方（邮费与成本合计之间）新增「吊牌成本合计」行。
- 公式：`吊牌成本合计 = 成本合计 − 邮费`（标准模式），即吊牌本身成本（纸张+工艺+吊绳，不含邮费运输）。
- 直接系数模式下 = 成本合计（本身就不含邮费）。
- 缺价时显示「部分缺价」。

## v8.8.0（2026-09-03）

### 修正默认报价隐藏行为

- 关闭「默认报价」开关时，邮费快速修改区的「修改邮费后报价」标签和 3 张客户等级卡片**正确隐藏**（v8.6 误把这三处都摘出来了）
- **「修改后成本」红字始终显示**，不受默认报价开关控制（这是你的硬性要求）
- 即：默认报价开关只控制「邮费标签 + 客户卡片 + 默认报价卡片」三处，「修改后成本」独立始终可见

## v8.7.0（2026-09-03）

### 输入临时系数时「修改后成本」保持显示

- 同时填写「临时毛利系数 + 邮费快速修改」时（v8.1 联动），原行为：邮费修改标签和 3 个客户等级卡片都隐藏，仅由临时系数卡片展示合并结果。
- 新行为：「修改后成本」红字**保持显示**，让用户能直接看到新成本是多少（再被临时系数相乘得到最终价）。
- 仅标签与 3 个客户等级卡片继续隐藏（避免与临时系数卡片重复）。

## v8.6.0（2026-09-03）

### 邮费快速修改区始终可见

- 邮费快速修改区（「修改邮费后报价」标签 / 「修改后成本」红字 / 报价卡片）不再随「默认报价」开关隐藏，始终保持可见。
- 仅「默认报价」标签与默认客户等级卡片受显隐开关控制。
- 同步修正资源引用缓存参数 `?v=8.6.0`。

## v8.5.0（2026-09-03）

### 修复空字符串价格 Bug（重要）

- **问题**：`data.js` 中纸张价格/直接系数有 106 处空字符串 `""`（v8.0 用 Excel 生成时，空单元格 `''` 未转 `null`），导致 `hasExactTier` 把 `Number("")===0` 误判为「价格 0」，部分档位（如 30000/100000/3000/4000/2500/7500）被按 0 元计价。
- **修复**：
  - 将 `data.js` 中 106 处空字符串统一改为 `null`（价格 80 处 + 直接系数 26 处）。
  - `hasExactTier` 增加 `v !== ""` 防御，避免未来 Excel 导入再引入空字符串误判。
- 验证：`paper2` 30000 档由「价格 0」修正为「无价格（缺价提示）」，`paper7` 直接系数空值修正为 `null`。

## v8.4.0（2026-09-03）

### 数据同步（纠正版 Excel）

- 用纠正版 Excel（`KOKALabel1楼.xlsx` / `KOKALabel3楼.xlsx`）作为最新权威数据源，重新确认纸张与工艺数据。
- 对比结果：纸张 44 张完全一致；3 楼工艺有 32 处「空单元格 vs null」的表示差异，统一为 `null` 后无实质价格变化。
- 新增 `migrateV8_4CorrectData` 迁移 + `dataVersion` 标记，首次升级时覆盖用户本地纸张/工艺数据为最新默认（此后用户自定义不再被覆盖）。
- 报价表显示名称保持「1楼」「3楼」。

## v8.3.0（2026-09-03）

### 邮费快速修改增强

- 修改邮费后新增「**修改后成本**」显示，红字高亮（与主结果区「成本合计」同风格）。
- 「修改后成本」= 原成本 − 原邮费 + 新邮费。
- 邮费快速修改区（标签 / 修改后成本 / 报价卡片）随「默认报价」开关联动：默认报价隐藏时一并隐藏，显示时恢复。

## v8.2.0（2026-09-03）

### Bug 修复（P1）

- **缺价不再生成正式报价**：标准报价的 `costKnown` 改为纳入全部缺价状态（`!hasMissingTier`），纸张/工艺/吊绳/邮费任一缺价时正确标记「部分缺价」，不再用 0 凑出成本生成报价。
- **面积 6000mm² 边界回落**：强制映射范围由 `5500–5999` 扩展为 `5500–6000`，6000 强制命中 `006`，不再回落到 `055` 导致价格下降。
- **邮费 Excel 导入持久化**：导入后写入 `localStorage`，刷新页面不再丢失。
- **快照恢复完整还原**：恢复快照时补上吊绳配置与邮费配置的还原（此前遗漏）。

### 其他优化

- **默认纸张真正生效**：计算器初始化纸张时优先使用个人主页设置的默认纸张（若存在于当前报价表）。
- **文案修正**：恢复默认提示「43 张」改为「44 张」，报价表名改为「1楼/3楼」；导出 JSON 版本字段 `6.5` 改为 `8.1.0`（本次提交后为 8.2.0）。

## v8.1.0（2026-09-03）

### 临时毛利系数与邮费快速修改联动

- 当**同时**填写「临时毛利系数」和「邮费快速修改」时，合并为一个最终价格：
  `最终价格 = (原成本 − 原邮费 + 新邮费) × 临时毛利系数`
- 此时隐藏独立的邮费修改卡片（3 个客户等级），仅显示合并后的临时系数卡片，标签带「新邮费」提示。
- 只填其中一个时，行为保持不变（临时系数 → 原成本 × 系数；邮费修改 → 新成本 × 客户等级系数）。

## v8.0.0（2026-09-03）

### 报价表结构重构

- 报价表从 4 个重构为 **2 个**：
  - **1楼**（`priceList1`）= 34 张纸（原 2 号 16 + 3 号 12 + 4 号 6）
  - **3楼**（`priceList2`）= 10 张纸（原 1 号）
- 小组名改为 **1楼小组 / 3楼小组**；报价表名改为 **1楼 / 3楼**。

### 纸张数据替换

- 按 Excel（`KOKALabel1号报价表`、`KOKALabel2号报价表`）覆盖全部 44 张纸的简称、折扣系数、直接系数、规格价格、批量直接报价。
- 纸张 id 保持不变，**工艺配置未替换**（保持原有数据）。
- 新增 `migrateV8Restructure` 迁移：检测旧 4 报价表结构后，自动覆盖纸张/报价表/邮费数据。

### 邮费重构

- 地区从 3 个扩展为 **9 个**。
- 档位改为 `500 / 1000 / 2000 / 5000 / 10000`（去掉 3000 档与「小面积 8 折」规则）。
- 新增「大于 10000」超量系数（`overTierCoeff`），计算逻辑分三种：
  - **固定档**：数量正好命中档位 → 直接查价；
  - **超量档**：数量 > 10000 → 弹对话框输入吊牌重量(kg)，邮费 = 重量 × 系数；
  - **中间档**：数量为 2500/3000/4000 等无定价档 → 弹对话框手动输入邮费。
- 新增两个输入对话框（重量 / 手动邮费），会话级缓存避免重复弹窗。
- 邮费的就地编辑、Excel 导入/导出/模板均适配新结构（`超量系数` 列替换原阈值/折扣列）。

### 其他

- 版本号 `v7.9.0` → `v8.0.0`。

# KOKALabel 报价系统 变更日志

## v10.6.0（2026-09-13，版本整合发版）

### 版本整合：同事 v10.5 合并入库，定位 v10.6.0
- 同事 v10.0 ~ v10.5 全部并入仓库 `archive/同事V10系列/`（V10.0/V10.1/.../V10.5 快照），工作树更新至 v10.5 代码，版本号定为 **v10.6.0**。
- 同事 V9 系列（9.0 ~ 9.9）并入 `archive/同事V9系列（vice）/`，全部目录加后缀「（vice）」与仓库 9.8 基线区分，仅作版本记录、不部署。

### 品牌与登录（用户拍板，v10.6 明确规则）
- **品牌保持 KOKA**：v10.5 同事版的「亲成」品牌/公司名改动全部回退，全站标题/侧栏/登录页/导出文件名/默认公司名保持 KOKALabel。
- **图标保持 KOKA**：`index.html` 图标引用改回 `assets/` 下 KOKA 菱形铅笔标（v9.8 图标）；删除根目录同事版亲成图标文件（favicon.svg/ico/png、apple-touch-icon.png、assets-logo-qincheng.svg），归档内保留亲成原档备查。
- **登录保持 9.8**：账号登录体系（login.html / js/auth.js / functions/ Pages Functions + KV，管理员/业务员/访客）采用 9.8 版，不使用 v10.5 的登录实现；auth.js 保留 v10.6.0 兼容补丁（`roleNum/canEditQuote/toast`）供 v10.5 app.js 调用。

### 9.8 功能保留与补全
- **补全「上传为默认数据」（管理员）**：`js/app.js` 新增 `initDefaultDataUpload()`（继承 9.8 实现），挂入 `safeInit` 启动序列；管理员在 数据管理 标题右侧上传当前 报价表/纸张/工艺/吊绳/邮费/客户等级 为全用户共享默认数据，其他账号下次登录自动应用（`/api/default` POST + KV 存储）。数据包不含 9.8 的 `priceListGroups`（v10.6 无在线修改功能，后端按缺省忽略）。
- 统一内联编辑提示：4 处过时提示「↑ 保存为全局默认」改为「上传为默认数据」（数据管理）。
- **功能差别标记文档**：新建 `docs/功能差别标记-9.8与10.6.md`，列出 9.8 独有、v10.6 已移除的功能（F1 在线修改 / F2 按模板改数据 / F3 报价表组 / F4 统计报表 / F5 覆盖明细 / F6 快照集合 / F7 授权门控遗留 / F8 完整性签名 / F9 安全函数严格版 / F10 sheet 名重名序号 / F11 迁移基线差异），作为后续版本加回的唯一标记入口。

### 测试适配（v10.6 代码基线）
- `tests/floor1-data.test.mjs`：迁移断言基线 `9.7.3 → 9.9.0`（同事版迁移链 `9.5.1→9.9.0`）。
- `tests/export-sheet-name.test.mjs`：适配 `sanitizeSheetName()`（替换 9.8 的 `SHEET_NAME_SANITIZE_MAP/toSafeSheetName`，差异见 F10）。
- `tests/security-hardening.test.mjs`：适配 v10.6 `validateImportedData` 内联校验与 `escapeHtml`（DOM mock），差异见 F9。
- `tests/online-edit.test.mjs`、`tests/stats.test.mjs`、`tests/integrity.test.mjs`：对应 9.8 功能已移除，整文件 skip 暂挂（加回功能后删除 skip 常量即恢复）。
- 全量测试结果：**26 通过 / 24 跳过（9.8 移除功能）/ 0 失败**。

### 修复（v10.6.0，同事版遗留 bug）
- `js/data.js` v9.5.1 迁移：同事版只写 localStorage 不同步内存 `PAPER_CONFIG/CRAFT_CONFIG`，老用户升级后**本会话残留旧 1 楼数据，需刷新才正常**；现已同步内存，与 9.8 迁移行为一致（见功能差别标记文档 F11）。

### 其他整理
- 清理根目录与归档重复的同事版文件：转手文档/、问题日志.md、预览截图/（归档 `archive/同事V10系列/V10.5/` 已有原档）。
- `.gitignore` 合并恢复（补回 node_modules/、*.tmp、.idea/、.vscode/ 等，保留 _backup_v8/）。
- 版本头统一：`js/data.js` v9.5.1 → v10.6.0、`css/style.css` v10.3.0 → v10.6.0、`index.html` 全站引用 v10.6.0。
- 全量验证：JS 语法检查通过、功能差别清单核对完成、git 提交版本管理（v8 分支）。

### 已知差异（不视为缺陷）
- v10.6 数据管理为 本地快照（含价格配置导入导出）/报价历史/云同步 三 tab；9.8 的「在线修改」tab 与统计报表按用户要求标记待加回（见功能差别标记文档 F1/F4）。

## v10.5.0（2026-09-11，已上线）

### 改造：纸张材质「下拉搜索」模式改为双列网格弹窗 + 只显示简称
- **选项只显示简称**：下拉列表原先每项显示「序号 + 简称 + 全称」（如 `1. 700米兰纹 / 1楼小组-1：米兰纹 700克 双面过光油`），现改为**仅显示简称**（`700米兰纹`）；完整名称保留在 `title` 属性，鼠标悬浮即可查看。
- **双列网格排布**：弹窗内选项由单列纵向列表改为 **2 列 Grid**（`grid-template-columns: repeat(2, minmax(0, 1fr))`），列表高度减半、相同屏幕内可容纳近两倍选项。
- **三种布局全部生效**：
  - 手机端（**垂直纵深式** `body.ui-vertical`）：2 列网格正常排布，弹窗不再下溢视口；
  - 电脑 / 平板（**扁平化宽泛式** `body.ui-flat`）：2 列网格正常排布，窄侧栏下同样不溢出。
- 触发器（已选纸张芯片）同步只显示简称，移除副标题全称，视觉更紧凑。
- 搜索框提示语改为「输入简称模糊搜索」，交互逻辑不变（实时模糊过滤、方向键/回车选择、Esc 关闭）。

### 修复：UI 框体溢出与规范问题（共 5 处）
1. **报价结果表格撑破整页（桌面端横向溢出 49px）**
   - 根因：`.result-section` 为 `display:grid` 容器，隐式列宽默认 `auto`，被 5 列表格按 `min-content` 撑到 493px，逐层传导（`.result-row` → `.result-table-scroll` → 超出 `.calc-result-col` 453px）→ 整页出现横向滚动条。
   - 修复：`.calc-result-col .result-section { grid-template-columns: minmax(0, 1fr) }` 显式约束列宽；表格外包 `.result-table-scroll{ width:0; min-width:100%; overflow-x:auto }`，表格过宽时在容器内横向滚动而非撑破页面。
2. **手机端导航按钮被挤出视口 13px**
   - 根因：`body.ui-vertical .sidenav-menu .nav-btn { flex: 1 }` 使三个按钮平分宽度后总宽超出容器。
   - 修复：改为 `flex: 0 0 auto; white-space: nowrap`（按钮自适应内容宽度）+ `.sidenav-menu { overflow-x: auto }` 允许横向滑动；文字不再被截断。
3. **手机端下拉弹窗下溢视口 71px**
   - 根因 1：`adjustDropdownPosition()` 计算可用高度时未扣除弹窗自身 `padding/border` 与搜索区高度；
   - 根因 2：`togglePaperDropdown()` 仅通过 `requestAnimationFrame` 调用定位，被浏览器节流时停留在 CSS 默认 `max-height: 300px`。
   - 修复：JS 改为**同步定位 + 下一帧复测**双保险，并扣除 `boxOverhead` 与 `searchH`、加 `Math.max(120, …)` 下限；CSS 追加 `max-height: min(300px, 52vh)` 兜底（≤520px 屏为 `46vh`）。
4. **桌面端「订单备注」textarea 右溢 30px**
   - 根因：`.quote-save-fields { grid-template-columns: minmax(180px, .7fr) minmax(260px, 1.3fr) }` 第二列最小宽 260px 强制撑破窄侧栏。
   - 修复：改为 `minmax(0, .7fr) minmax(0, 1.3fr)`；窄侧栏（`.calc-result-col`）内强制单列。
5. **全局防溢出兜底**
   - `.app-shell / .pages / .page / .calc-grid / .calc-form-col / .flat-section / .sheet-card` 统一补 `max-width: 100%; box-sizing: border-box`；`.pages` 加 `overflow-x: clip`。
   - 下拉弹窗宽度规范：`min-width: min(420px, calc(100vw - 24px)); max-width: calc(100vw - 24px)`。

### 验证
- 三视口实测：`desktop 1440×900 (ui-flat)` / `tablet 768×900 (ui-flat)` / `mobile 390×780 (ui-vertical)`
  - 页面横向溢出均 **0px**（`docScrollW == clientW`）；真实溢出元素计数 **0**；
  - 下拉选项 66 项，首项简称 `700米兰纹`，全称节点 `.paper-desc` 已不存在，前两项同行（双列确认）**YES**；弹窗下溢/右溢均 0px。
- 叠加 14 组场景矩阵（扁平/垂直 × 网格/下拉 × 三视口）全部通过。
- 版本串：`index.html` 16 处、`app.js` 6 处、`style.css` 注释 8 处升为 `10.5.0`；`data.js` 迁移守卫串 `"9.9.0"` 未被污染。
- JS 语法（`app.js` / `data.js` / `auth.js`）全部通过。

## v10.3.0（2026-09-10 晚，本地修改）

### 修复：吊绳类型展开后芯片排布异常
- 根因：v10.1 折叠样式用 `display:flex` 覆盖了原三列 `grid`，27 个吊绳被压成一行细竖条。
- 修复：折叠仅控制隐藏（`.rope-fold:not(.open) .rope-list{display:none}`），展开时完全保持原有三列网格布局。

### 个性化：显示系统默认字号与实际字号 + 自定义输入
- 面板新增说明行：**系统默认字号 16px（100%）｜ 当前字号 X px**（实时换算，随缩放档位/自定义值更新）。
- 自定义百分比输入（50~200%）与快捷档位、恢复默认按钮保留。

### 报价表组查询：表头/行列改为表内内联编辑（超管）
- 移除 v10.2 的独立「表头编辑」卡片，改为**在价格表上直接编辑**（与改价格同款交互）：
  - **列（档位）**：点击档位表头直接输入新档位名（价格保留，作用于纸张规格/附加工艺/吊绳/邮费表）；表头内 ✕ 删除该列；表头末尾 **＋** 新增档位列（各表该列初始为空）。
  - **行（规格）**：代码列与最大面积列可点击直接修改（代码保持文本、唯一性校验；面积正数校验）；行尾 ✕ 删除规格行；表格下方「＋ 新增规格行」按钮自动生成空闲代码。
- 所有修改沿用价格编辑同款确认对话框，仅存本地；「↑ 保存为全局默认」核对后上传云端。

## v10.2.0（2026-09-10 晚，本地修改）

### 修复：个性化面板卡死 + 取消开屏弹窗
- 根因：`.modal-mask{display:flex}` 覆盖了 `hidden` 属性的 `display:none`，遮罩层开屏即全屏常驻、拦截全部点击（表现为"弹窗卡死"）。
- 改造：个性化改为**内联下拉面板**（菜单栏按钮旁弹出，无遮罩层），点击按钮开/关、点击面板外部自动关闭。

### 修复：工艺费并入单纸张价后乘折扣（标准报价模式）
- 旧逻辑：纸张价先乘折扣，工艺费单独累计直接叠加（工艺不打折）。
- 新逻辑：每张纸 **(纸张价 + 工艺费) × 折扣**（再乘面积系数），工艺费不再单独参与合计；成本合计 = 纸张折后价(含工艺) + 吊绳 + 邮费。
- 明细行「附加工艺费」保留展示但标注"（已含于纸张折后价）"；折扣/面积系数的计算过程展示自动适配。
- 直接系数模式维持 v7.2 逻辑（(纸张+工艺)×系数）与原成本口径，不受影响；批量直接报价纸张不变。

### 新增：报价表组查询页表头（批量档位）编辑（仅超级管理员）
- 新增「表头（批量档位）编辑」卡片：**新增档位**（各表价格默认为空，点击单元格填写）、**重命名档位**（保留价格）、**删除档位**（二次确认，至少保留一档）。
- 作用范围：当前报价表组的纸张规格 + 附加工艺 + 吊绳 + 邮费表（全页表头统一）。
- 修改仅存本地（与内联价格编辑同机制），**核对后手动点「↑ 保存为全局默认」确认上传云端**——满足"确认之后再保存为全局默认"要求。

## v10.1.0（2026-09-10 晚，本地修改）

### 个性化：菜单栏字号调整（所有用户可用）
- 左侧菜单栏底部新增「Aa 个性化」按钮 → 弹出面板：字号档位（小 85% / 默认 100% / 大 115% / 特大 130%）+ **恢复默认**按钮 + 当前百分比显示。
- 通过 CSS zoom 缩放整页界面；偏好存 localStorage（uiFontScale）仅本机生效；**不受账号权限影响**（访客亦可使用）。

### 吊绳类型折叠组件（默认折叠）
- 计算器「吊绳类型」板块改为可折叠：标题行显示箭头 + 当前选中吊绳摘要（含 ¥基准/千张），点击/回车展开收起，**默认折叠**。折叠不影响计价（默认吊绳已选中）。

### 纸张材质下拉框模式（与芯片平铺共存）
- 个人主页 → 报价设置 → **纸张材质选择模式**（芯片平铺（默认）/ 下拉框搜索），切换后保存即重绘。
- 下拉模式：触发框显示「简称芯片 + 全称芯片」同一行；面板顶部搜索框支持简称/全称**模糊搜索**（多关键词、忽略大小写、复用 v9.5 时代的 paperMatchesSearch 匹配引擎），Enter 选首个匹配、Esc 关闭、点外部收起；选项列表每项同为「简称芯片 + 全称」同行，选中高亮。

### 品牌更换：KOKALabel → 亲成 + 全套图标更新
- 全站用户可见文案（标题/侧栏品牌/登录页/导出文件名/默认公司名等 20 处）改为「亲成」。
- 内部标识符（KOKA 对象、KOKA_AUTH KV、tagPricing_ 存储前缀、koka* 类名）**保持不变**，存储与云同步完全兼容。
- 图标更新：新 logo SVG 入库（assets-logo-qincheng.svg）；favicon.svg（图形部分白底圆角）新增引用；favicon.ico（16~256 多尺寸）/ favicon.png(32) / apple-touch-icon(180) 全部由新 logo 栅格化重新生成（Edge headless 截图 + Pillow）。

## v10.0.0（2026-09-10，本地修改）

### 计价模式：标准报价前置 + 默认标准报价
- 工具条切换按钮顺序调整为「标准报价 | 直接系数计算」，默认选中**标准报价**（未手动切换过的新/老用户均生效；曾手动切换过的仍记忆其选择）。

### 纸张材质序号可隐藏（个人主页开关，默认隐藏）
- 个人主页 → 报价设置 → **纸张材质序号**（隐藏/显示），默认**隐藏**；APP_PROFILE.hidePaperIndex（旧配置自动补默认隐藏）。
- 切换后保存即重绘纸张芯片；开启显示时仅当材质数 >10 才出现编号（延续原规则）。

### 表单字段标题显眼化
- 「纸张材质」「吊牌展开尺寸」「附加工艺」（动态+静态）加 field-label 样式：蓝色竖条 + 浅蓝底胶囊 + 深色粗体；「吊绳类型」等分区标题（flat-section-title）同步加浅蓝底，四个标题视觉统一。

### 过程教训（再次踩中并即时修复）
- 版本号批量替换误伤 data.js 迁移基线串（migrateRopeV99 的 guard/write "9.9.0" 被改成 "10.0.0"）——已恢复。**规则强化：替换脚本必须将 data.js 的迁移串（verCompare/dataVersion 写入值）列入豁免**。

## v9.9.0（2026-09-10，本地修改）

### 吊绳价格倍数制（1000 张基准）

- **计算逻辑**：吊绳以 1000 张为基准价；**500 张与 1000 张同价**；其余档位按 `基准价 × (档位/1000)` 计算，不再要求报价表存在精确档位（3500、7500 等任意档均可计算）。例：普通吊绳基准 ¥10 → 500 张 ¥10、3500 张 ¥35、10000 张 ¥100。
- **结果展示**：档位 >1000 时吊绳明细行显示计算过程 `吊绳名（¥10/千张 × 3.5 = ¥ 35.00）`。
- **模板数据**：DEFAULT_ROPE_CONFIG 27 组吊绳的 500 档价格统一改为与 1000 档同价（原 500 档为半价）。
- **老用户迁移**：migrateRopeV99（dataVersion→9.9.0）自动把本地已存吊绳数据的 500 档改为基准同价；云端共享数据由超管重新「保存为全局默认」即可同步新规则。
- 基准价取值：优先 1000 档，缺省回退 500 档；均无则提示「未设置 1000 张基准价」。

## v9.8.0（2026-09-10，已部署）

### 面积系数单价显示计算过程
- 报价结果「各纸张尺寸/面积/代码」表的单价列：面积超 10000mm² 且报价表无对应面积代码（启用面积系数）时，由 `¥ 130.00` 改为显示 **`¥ 130.00 × 1.27 = ¥ 165.10`**（基准价 × 面积系数 = 实际价，结果蓝字加粗），与下方明细行格式统一。

### SKU 芯片长名称完整显示
- 纸张材质芯片去掉单行截断（ellipsis），改为自动换行完整显示全部文字；列宽 minmax 96→104px，字号 11.5px 自适应换行。

### UI 架构模式：移动端垂直纵深式 / 桌面·iPad 扁平化宽泛式
- **跟随设备（默认）**：手机 UA 自动启用「垂直纵深式」（顶部渐变导航横条 + 参数/结果单栏纵排 + 卡片观感还原 v9.6 风格）；电脑/iPad 用「扁平化宽泛式」（左侧竖栏 + 双栏）。
- **手动切换**：个人主页 → 报价设置 → **UI 架构模式**（跟随设备/扁平化宽泛式/垂直纵深式），选择后本机立即生效。
- 偏好存 localStorage 按设备生效（不入 APP_PROFILE 云同步，避免跨设备互相干扰）；实现为同一 DOM + body.ui-vertical/ui-flat class 切换，业务逻辑零改动。

### 发布
- v9.7.1（扁平化重构）与 v9.8.0 均推送 GitHub（分支 v9.7-flat / v9.8-dev，tag v9.7.1 / v9.8.0）。
- v9.8.0 部署 Cloudflare Pages（wrangler v3 direct upload）。

## v9.7.1（2026-09-08，仅本地，不部署）

### 「扁平化宽泛式」UI 重构（计算器页）

- **导航移出顶部**：顶部横条 → 左侧固定竖栏（品牌区 + 3 个页面入口垂直排列）；登录账号条随迁侧栏底部。窄屏（≤1100px）自动退化为顶部横条。
- **垂直纵深 → 扁平宽泛**：计算器页改双栏——左栏参数表单（宽）+ 右栏报价结果（sticky 跟随），主区宽幅利用；窄屏单栏堆叠。
- **渐进式卡片 → 平铺直列表单**：参数区去卡片外壳，改为「工具条（模式/报价表/默认报价开关）+ 报价条件（档位/纸张数量/收货地区一行三列）+ 纸张设置 + 吊绳类型」分区平铺，小节标题蓝色竖条化，行式紧凑（label 左置、间距压缩）。
- **弱化动态组合配置**：纸张材质「点击展开→搜索→选择」渐进下拉整体移除（paper-dropdown/搜索框退场；相关函数保留为死代码无入口）。
- **强化预设 SKU 快速点选**：纸张材质改为 SKU 芯片网格直选（点一下即选中，全称悬浮 title 显示），附加工艺/吊绳芯片同步紧凑化。
- **换皮不换骨**：全部元素 id/计算逻辑/云同步/导入导出零改动；备份 JSON 版本字段同步 v9.7.1；data.js dataVersion 基线 9.5.1 未动。
- 本版本仅本地修改与归档，**不部署 Cloudflare Pages**；生产文件复制至 `C:\Users\Mayn\Desktop\KL\9ge tebieban\9.7.1`。

## v9.6.0（2026-09-09）

### 「尺寸类型」改为全局设置（个人主页-报价设置）

- 计算器纸张条目中的「类型（单张尺寸/展开尺寸）」下拉**移除**。
- 新增位置：**个人主页 → 报价设置 → 默认选项 → 默认尺寸类型**（单张尺寸/展开尺寸），对所有纸张全局生效。
- 逻辑适配：新建/切换报价表/导入恢复时尺寸类型均取全局设置；个人主页修改并保存后立即同步全部纸张并重算。
- 兼容：旧 appProfile 自动补 defaultSizeType=single，行为与旧默认一致。



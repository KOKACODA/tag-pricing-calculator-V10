# KOKALabel 报价系统 · 部署与运维日志

> 记录版本上线、数据更新、部署状态。敏感信息（账号/密码/密钥/令牌）不写入本文件，统一存于仓库外凭据备份。

## 2026-09-13

### v10.6.0 版本整合发版（待部署确认）⏳

- **整合**：同事 v10.0 ~ v10.5 归档 `archive/同事V10系列/`，工作树更新至 v10.5 代码并定为 **v10.6.0**；同事 V9 系列（9.0~9.9）归档 `archive/同事V9系列（vice）/`（后缀 vice 区分，仅记录不部署）。
- **品牌/登录规则（用户拍板）**：品牌与图标保持 KOKA（亲成改动全部回退）；登录体系保持 9.8（login.html / auth.js / functions/），不使用 v10.5 登录。
- **补全**：`app.js` 新增 `initDefaultDataUpload()`（继承 9.8 上传为默认数据）；新建功能差别标记文档 `docs/功能差别标记-9.8与10.6.md`（9.8 独有功能待加回清单 F1~F11）。
- **修复**：`data.js` v9.5.1 迁移不同步内存 bug（老用户升级后本会话残留旧 1 楼数据，需刷新才正常）——现已同步 `PAPER_CONFIG/CRAFT_CONFIG`。
- **验证**：JS 语法检查通过；全量测试 **26 通过 / 24 跳过（9.8 移除功能，含在线修改/统计/完整性签名，待加回后恢复）/ 0 失败**。
- **发版方式**：Git 自动部署仍断，按惯例 `wrangler pages deploy . --project-name=tag-pricing-calculator-v5 --branch=v8` 直传；部署后验证生产标题 `v10.6.0` + KOKA 图标 + 登录页正常。

## 2026-09-09

### v9.8.0 数据管理新增「在线修改」标签页 ✅

- **功能**：个人主页 · 数据管理新增第 3 个标签页「在线修改」（报价历史与云同步之间）——区块一按组卡片管理报价表组/报价表（新增/重命名/删除/归属移动，删除表联动清理纸张与工艺）；区块二管理员按模板改数据（选定报价表 → 导出含现有数据模板 → 导入即原地覆盖，`applyPriceListData` 先清后合，区别于价格配置页的「导入=新增表」）。
- **权限**：http 部署仅管理员可见完整界面（其他角色显示锁定提示），file:// 离线放行。
- **数据层**：`PRICE_LIST_GROUPS` 持久化 + 7 个管理函数；`addPriceList` 支持 groupId/switchTo 并修 ID 同毫秒碰撞；组配置覆盖本地备份/完整配置/快照/重置/云下发全链路（`validateImportedData` 增组结构校验，旧文件向后兼容）。
- **修复**：`paperToSheetRows` 参数化，可导出任意报价表模板；导出元信息「所属小组」改动态 `getGroupName()`。
- 测试：新增 `tests/online-edit.test.mjs`（12 用例），全站 57/57；全站版本 v9.8.0；已推送 GitHub（v8 + main）+ wrangler 直传上线（生产验证：标题 v9.8.0、在线修改标签页与 `applyPriceListData` 均已生效）。

### v9.7.3 1楼默认报价表变更 + 迁移守卫修复 ✅

- **数据**：按最新 Excel（KOKALabel1楼_20260908_2227.xlsx）替换 1 楼默认报价表为 33 组（合并纸拆分 + 新增 5 组），3 楼 10 组默认不动；`migrateV9_7_3ReplaceFloor1()` 保证老用户 3 楼（含改价）保留、1 楼整体替换、1 楼工艺按新默认重建。
- **守卫修复（关键）**：v8.4 / v9.0.1 迁移守卫原为 `===` 判断，"9.0.1" 用户每次加载都会重跑 v8.4 并抹掉 3 楼用户改价（v9.0.1 上线起即存在的隐患）。新增 `compareDataVersion()` 语义比较，三处守卫改 `>= 0` 跳过。
- 测试：新增 `tests/floor1-data.test.mjs`（5 用例），全站 45/45；全站版本 v9.7.3；wrangler 直传上线。

## 2026-09-08

### v9.7.2 1楼导出修复 + 附加工艺标题高亮 ✅

- **Bug**：1 楼报价表导出失败——6 张多材质合并纸张简称含半角 `/`（2 张另超 31 字符），SheetJS 追加 Sheet 抛错；与 900 铜版纸修正无关。新增 `toSafeSheetName()` 清洗（全角映射/截断/重名序号），「导出报价表」「下载空白模板」两处接入；导入按内容元信息匹配不受影响。
- 附加工艺折叠标题红色高亮（对齐 v9.5.2 吊绳样式，规则统一 `#page-calculator .collapse-toggle`）。
- 新增 `tests/export-sheet-name.test.mjs`（4 用例），全站 40/40；全站版本 v9.7.2；wrangler 直传上线。

### v9.7.1 文档更新 + 目录整理 ✅

- 五文档对齐 v9.7.1（AGENTS / HANDOFF / 项目总结 / 总档案 / CHANGELOG），README / AGENTS 同步。
- 根目录图标下沉 `assets/`（index.html 引用改路径；login.html 补 favicon 显式引用）。
- `git log --diff-filter=D` 复核：历史无文件删除，无误删。
- 全站版本号 v9.7.1；wrangler 直传上线（生产部署验证通过）。

### v9.7 生产部署（wrangler 直传）✅

- **背景**：GitHub 仓库改名为 `tag-pricing-calculator-V10` 后，Cloudflare Pages 的 Git 集成断链（生产分支配置丢失、推送不再触发构建），线上停留在 v9.5.3（v9.6/v9.7 已推送 GitHub 但未部署）。
- **处理**：本地 remote 更新为新仓库名；用 API 将项目 `production_branch` 重设为 `v8`；`wrangler pages deploy` 直传 v9.7（部署 ID `e3c33ace`，生产环境），Functions 与 KV 绑定保持不变。
- **验证**：生产域名标题/页内标注/JS 资源版本头均为 v9.7。
- **后续发布方式**：Git 自动部署暂不可用，需 wrangler 直传（或面板重新绑定新仓库 `tag-pricing-calculator-V10` 恢复自动构建）。
- **安全提醒**：API Token 曾在对话中传输，建议在 Cloudflare 面板轮换。

### v9.7 临时修改计算统一单源 + 历史列表事件委托 ✅

- 新增公共计算函数 `computeStandardOverridePrice` / `computeDirectTempTotals`：屏幕卡片/明细与保存记录（snapshot.override）共用同一份公式，改规则只改一处，杜绝「显示价 ≠ 保存价」；保存数据格式与 v9.6 完全一致。
- `renderHistory` 改表格级事件委托（单一 onclick），替代逐行绑定，几百条历史时渲染更快；顺带修复**删除历史记录后统计报表不刷新**（补 `renderStats()`）。
- 勘误：上版建议「载入历史回填临时修改」经查为误报，该功能自报价历史初版（`f738d9e`）已实现，本次仅验证。
- 测试：quote-history.test.mjs 新增 2 用例（公共函数四分支/每纸系数计算），全套 36 用例通过。
- 版本号全站升至 v9.7。

### v9.6 保存报价固化临时修改 ✅

- 修复：调整「临时毛利系数 / 邮费快速修改 / 每纸临时直接系数」后保存报价，临时修改现随记录固化（`snapshot.override`），历史列表与统计利润优先显示**修改后价格**，历史详情新增「临时修改（保存时生效）」红色区块。
- 兼容：无临时修改不写入该字段，旧记录读取行为不变；override 价格非法自动回退原价。
- 测试：`tests/quote-history.test.mjs` 新增 2 用例（override 写入深拷贝 / 修改后价格优先读取 6 分支），全套 34 用例通过。
- 顺带修复：`getHistoryRecordPrimaryPrice` 对 `Number(null)===0` 的误判（先判空再转数字）。
- 版本号全站升至 v9.6。

### v9.5.3 结果表单价列恢复面积系数算式 ✅

- 面积超 10000 mm²（无对应档位、启用面积系数）时，结果表「各纸张尺寸 / 面积 / 代码」的单价列显示「¥ 基础价 × 系数 = ¥ 实际价」（如 `¥ 85.00 × 1.58 = ¥ 134.30`），与纸张价合计行格式一致。

### v9.5.2 切换按钮顺序 + 吊绳高亮对象修正 ✅

- 「标准报价」切换按钮移到「直接系数计算」之前（默认模式仍为标准报价）。
- 吊绳红色高亮对象由吊绳**选项**修正为折叠标题「吊绳类型 点击展开」（15px 加粗红色）；选项恢复默认样式，选项右上角初始价格红色小字保留。
- 全站版本号升至 v9.5.2（含 README / HANDOFF-v8 分支表补同步）。

### v9.5.1 尺寸类型移入个人主页 + 默认模式改为标准报价 ✅

- 尺寸类型「单张尺寸/展开尺寸」从智能计算器每张纸移除，移至「个人主页-报价设置-默认选项」作为全局默认值（新建纸张自动使用）。
- 默认显示模式改为「标准报价」，不再是直接系数计算（按钮已调换，`calcMode` 默认值改为 `standard`）。
- 参数输入-吊绳类型：字体加大（15px）并高亮红色（`#b91c1c`），仅计算器页面生效。
- 提交 `261fd6d`（功能）+ `5cecc0f`（版本号统一）+ `5799f1b`（style.css 版本头 / CHANGELOG 笔误修正）；均已推送 `v8` 与远程 `main`，tag `v9.5.1`。
- 部署勘误：会话中断前 09:34 那次 `wrangler pages deploy --branch main` 实际上传的还是 v9.5.0 的文件内容（v9.5.1 改动当时尚未完成），生产域名一度停留在 v9.5.0。本次检查后重新执行 `npx wrangler pages deploy . --project-name=tag-pricing-calculator-v5 --branch main`，生产 https://tag-pricing-calculator-v5.pages.dev 已确认 **v9.5.1**（标题 / `css/style.css` 版本头 / `app.js` 默认 `calcMode=standard` 三项均验证）。

### v9.5.0 账号登录体系上线 ✅

- 默认数据：`1楼小组-22` 蒙版纸 → **900克 A级铜版纸**（厚度1.05mm）。
- **账号体系上线**（Cloudflare Pages Functions + KV `AUTH`）：管理员/业务员/访客三级角色；首次初始化（`SETUP_KEY`）创建默认管理员 **KOKA / 12345677**（昵称「老板」）并直接返回会话；管理员可管理子账号（昵称/密码/时长/启停/删除），默认保留至少一个管理员；**启停 bug 修复**（按钮文本=将执行动作，登录/会话双重校验 `enabled`，停用后立即无法登录且已有会话失效）。
- 默认数据上传（仅管理员）到 KV `default:quote`，全员下次登录自动应用；访客仅见智能计算器，业务员不可改默认数据。
- UI：附加工艺/吊绳类型默认折叠。
- 部署：`wrangler pages secret put` 持久化 `SETUP_KEY/ADMIN_USER/ADMIN_PASS/ADMIN_NICK` 4 个变量；`wrangler pages deploy`（`pages_build_output_dir="."` 使 `wrangler.toml` KV 绑定生效）上线生产 `main`；线上初始化 KOKA 管理员并全流程验证通过（登录/建号/启停/权限/默认数据上传拉取）。
- 修复：`listUsers` 改用用户名索引 `u_index` + 强一致 `get`，消除 KV `list` 传播延迟（初始化后立即登录提示未初始化、新建账号列表不刷新）。
- 提交 `f6460bd`（feat）+ `a6e8722`（fix）；tag `v9.5.0`。

## 2026-09-05

### v9.4.2 main 同步 + 历史档案 + 目录规范 ✅

- `main` 分支 force 同步到 `v8`（旧 v7.10 谱系经本地备份 tag `archive/v7.10-main` 保留）。
- 新增 `docs/项目历史与技术总档案.md`（全量历史 + 技术 + 转手方案）；`README` / `AGENTS` 分支说明同步修正。
- 全站版本号 v9.4.2。
- 部署：`wrangler pages deploy` 直传已上线（2026-09-06），生产域名 https://tag-pricing-calculator-v5.pages.dev 标题确认 v9.4.2；提交 `21a53bc` 已推送 `v8` 与 `main`，并打 tag `v9.4.2`。

### v9.4.1 快照与统计报表合并框体 + 导出 + 清除缓存 ✅

- 「本地快照列表」与「统计报表」合并为独立卡片「快照与统计报表」，置于个人主页「数据管理」下方；「本地快照」标签更名「价格配置」；统计报表由弹窗改为内联渲染。
- 新增「导出快照」（签名 JSON）与「导出统计报表」（XLSX 三工作表）；均本机下载、不上传。
- 新增「一键恢复默认并清除缓存」（删除报价历史 + 本地快照）；保留原「一键恢复全局默认设置」（不影响历史与快照）。
- 部署：`wrangler pages deploy` 直传（`--branch main`）；推 GitHub 后需手动部署（Git Provider = No）。

### v9.4.0 统计报表（本地，不上传）✅

- 定位为 v9.4 版本（区别于 v9.1 数据基线）：「更多功能 → 统计报表」占位卡片启用，打开独立统计框体。
- 三项指标：月度报价次数、热销尺寸（宽×长）、利润分布（建议报价 − 成本）。
- 数据来源：本机 `localStorage` 已保存报价历史；纯前端 CSS 条形图，零第三方 CDN，符合 CSP `script-src 'self'` 与离线双击使用；不上传云端。
- 部署方式：`wrangler pages deploy` 直传；推 GitHub 后需手动部署（本项目为直传项目，Git Provider = No）。
- 结果：提交 `1277292` 已推送 `v8`，并已直传上线；生产域名 https://tag-pricing-calculator-v5.pages.dev 标题确认 **v9.4.0**。

### v9.1.0 生产部署完成 ✅

- 生产域名 https://tag-pricing-calculator-v5.pages.dev 已切换至 **v9.1.0**（`index.html` 标题与 `js/data.js` 版本头均确认 v9.1.0）。
- 内容：1楼最新 **28 个报价组**（v9.0.1 数据基线）＋ P0/P1 安全加固（XSS 转义 / 导入校验 / 响应头 / 数据防篡改签名 + 前端口令门）。
- 部署方式：`wrangler pages deploy` 直传（`--branch main` 生产分支）。
- 根因说明：项目 `tag-pricing-calculator-v5` 为**直传项目（Git Provider = No）**，推 GitHub 不会自动触发部署；生产分支为 `main`。
- Cloudflare 账户：`KOKA`（account_id `a4fdb353836f801c65072d7810fbcc32`）；本次令牌关联邮箱 `2996978242@qq.com`。

### 后端保留说明（重要）

- Master 明确：「不要后端」= **不使用**，并非删除。
- 后端源码 v9.2 / v9.3（`functions/` 账号系统、`login.html`、`js/auth.js` 等）**未删除、未部署、未启用**，仍完整保留于 `/data/user/work/v9_full/V9/{9.2,9.3}`。
- 已另做版本备份压缩包：`/workspace/KOKALabel-后端版本备份-v9.2-v9.3.tar.gz`（964KB，含 9.2/9.3 全部源码）。

### 版本与数据

- 代码基线：GitHub `KOKACODA/tag-pricing-calculator-v5` · 分支 `v8`。
  - `bac88c4`：合并 v9.0.1 数据（28 报价组）与安全加固 → v9.1.0。
  - `ae1c301`：新增本部署日志。
- 价格数据：与 v9.2.0 / v9.3.0 的 `data.js` 逐行比对一致（9.2/9.3 仅新增授权/后端账号能力，无价格变化）。

### 凭据

- 后端管理员初始凭据 + SETUP_KEY、部署基准账号：存于仓库外 `/workspace/KOKALabel-凭据备份-勿提交.md`，严禁入库。
- 本次 Cloudflare 部署令牌仅临时使用，未落盘。